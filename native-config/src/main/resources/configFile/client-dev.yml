server:
  port: 8999


eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka/ #注册中心的访问地址
  instance:
    prefer-ip-address: true #是否将当前服务的ip注册到eureka server中
name: nativevonfig